#!/bin/sh

set -e

#for prog in aspell bash bin elvis mutt readline tmux vim x ; do
for prog in aspell bash bin elvis i3 readline tmux vim x ; do
    printf 'Configuring %s\n' "$prog"

    xstow "$prog"
done

printf 'Finished!\n'
